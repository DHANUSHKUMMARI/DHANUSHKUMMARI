use dhanush;
create table to_do_list (task_id int primary key,task_name varchar(255),description varchar(255),is_completed varchar(255));
insert into to_do_list (task_id,task_name,description,is_completed)values(1,'yoga','evening','no');
insert into to_do_list (task_id,task_name,description,is_completed)values(2,'brushing teeth','morning','yes');
select * from to_do_list;
